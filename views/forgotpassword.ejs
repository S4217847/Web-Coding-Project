<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Forgot Password | RMIT Connect</title>
    <link rel="stylesheet" href="/css/common.css" />
    <link rel="stylesheet" href="/css/password.css" />
    <link rel="stylesheet" href="/css/navigation.css" />
  </head>

  <body id="top">
    <%- include("partials/global-navigation", {
      activePage: "account",
      sessionAction: "login"
    }) %>

    <main id="main-content" class="forgot-password-page">
      <div class="forgot-password-card">
        <h1>Forgot Password?</h1>

        <p class="forgot-password-text">
          Enter the email address connected to your account.
        </p>

        <form
          id="forgot-password-form"
          class="forgot-password-form"
          action="/forgot-password"
          method="post"
        >
          <label for="reset-email">Email Address</label>

          <input
            id="reset-email"
            type="email"
            placeholder="name@example.com"
            name="reset-email"
            maxlength="120"
            required
          />

          <div class="error" id="email-error-text"><%= emailError %></div>

          <button class="resetbutton" type="submit">Continue</button>
        </form>

        <% if (resetMessage) { %>
          <p class="reset-message" role="status"><%= resetMessage %></p>
        <% } else { %>
          <p class="reset-message">
            The same response is shown whether or not an account exists.
          </p>
        <% } %>

        <% if (resetLink) { %>
          <p class="reset-message">
            Local demonstration link:
            <a href="<%= resetLink %>" class="common-link">reset this password</a>.
          </p>
        <% } %>

        <p class="account-help">
          Remember your password?
          <a href="/login.html" class="common-link">Log in</a>
        </p>
      </div>
    </main>
    <script src="/js/forgotpassword.js"></script>
  </body>
</html>
